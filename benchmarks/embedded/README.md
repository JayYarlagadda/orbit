# Embedded benchmark artifacts (planned)

Results for E2/E8 milestones. Categories:

| Directory | Measures |
|-----------|----------|
| `realtime/` | Sample rate, jitter, ISR latency, missed samples |
| `dma/` | Polling vs DMA CPU and latency comparison |
| `storage/` | Flash log throughput, recovery time |
| `reconnect/` | Offline duration, replay throughput, data loss |

Publish summaries under `docs/results/embedded/` when available.
